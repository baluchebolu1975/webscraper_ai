# Data Directories

This directory contains scraped and processed data.

## Structure

- `raw/`: Raw scraped data
- `processed/`: Processed and analyzed data

## Notes

- These directories are gitignored by default
- Data files are not committed to version control
- Only structure files (.gitkeep) are tracked
